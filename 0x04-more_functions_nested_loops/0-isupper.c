#include "main.h"

/**
 * main - checks if a character is uppercase or not
 * @c: character to be tested
 * Return: 1 whether it is, 0 otherwise
 */

int _isupper(int c)
{
if ((c >= 'A') && (C <= '2'))
{
return (1);
}

return (0);
}
